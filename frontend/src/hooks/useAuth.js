// Re-export useAuth from AuthContext for convenience
// Allows: import { useAuth } from '../hooks/useAuth'
// instead of: import { useAuth } from '../contexts/AuthContext'
export { useAuth } from '../contexts/AuthContext'
